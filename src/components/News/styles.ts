import styled from 'styled-components';

export const Container = styled.div`
  display: flex;
  flex-direction: column;

  > span {
    font-size: 14px;
    color: var(--gray);
    margin-bottom: 3px;
  }

  > strong {
    font-size: 14px;
  }

`;
